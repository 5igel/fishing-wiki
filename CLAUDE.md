# CLAUDE.md — Схема рибальської вікі Сергія

Це конфігурація для ведення персональної вікі про рибалку. LLM — єдиний редактор папки `docs/`. Сергій постачає джерела (фото, лістинги, нотатки з берега) і ставить питання; LLM веде сторінки, проставляє перехресні посилання, фіксує протиріччя.

## Структура
- `raw/` — сирі джерела (фото уловів, скріни лістингів, нотатки). Незмінні. (Поза сайтом Pages — публікується лише `docs/`.)
- `docs/` — markdown-сторінки, які веде LLM (вони ж = сайт GitHub Pages, source `/docs`).
- `docs/index.md` — каталог усіх сторінок з однорядковим описом (= головна сайту).
- `docs/log.md` — хронологічний лог (формат: `## [РРРР-ММ-ДД] тип | Назва`).
- `docs/_config.yml` — конфіг Jekyll для GitHub Pages (не сторінка вікі).

## Категорії сторінок
- **Водойми** (`waters-*.md`) — де ловив: глибини, риба, дистанція, нотатки
- **Спорядження** (`gear-*.md`) — поплавці, ліска, гачки, фурнітура
  - **Вудки:** одна вудка = одна сторінка `gear-rod-<slug>.md`; **котушки** — `gear-reel-<slug>.md`; **поплавці** — `gear-float-<slug>.md`. Хаби-каталоги: `gear-rods.md` (вудки+котушки), `gear-floats.md` (поплавці) — таблиця + спільні принципи; окремі сторінки лише під фактично наявне.
  - **Заповнювати СТРОГО за шаблоном** `templates/gear-rod.md` / `templates/gear-reel.md` / `templates/gear-float.md` (порядок секцій: **Короткий опис → Замітки → Історія використання**; короткий опис — булетами; без front-matter).
- **Риба** (`fish-*.md`) — види, які ловив: впізнання, вага, поведінка
- **Техніка** (`technique-*.md`) — огрузка, стилі, в'язання, виважування
- **Покупки** (`shopping.md`) — що куплено, що в процесі, що варто спробувати
- **Постачальники** (`suppliers.md`) — магазини, оцінка, доставка

## Правила
1. Кожне нове джерело/відповідь інтегрується в наявні сторінки, а не просто додається.
2. Протиріччя й уточнення фіксувати явно (напр. «номінал 4.5 = реальні 4.2»).
3. Оновлювати `index.md` і `log.md` при кожній зміні (файли в `docs/`).
4. Профіль рибалки: китайсько-корейський стиль, 1 гачок (польські правила), Краків, мах, тісто, карась + лин/лящ.
5. Мова сторінок — українська.
